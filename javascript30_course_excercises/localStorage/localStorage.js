const addItems = document.querySelectorAll('.add-items');
const itemsList = document.querySelectorAll('.plates');
const items = [];

function addItem(e) {
  console.log('helo');
}


addItems.addEventListener('submit',addItem);